import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    scrollPicker: {
      marginVertical: 10,
      width: 250,
      color:'black',
      alignSelf: 'center',
    },
});